# <项目名> — 执行进度追踪 (TodoList)

> **断点续跑约定**：每个 Agent 启动后第一步是读本文件，定位本 lane（A 行 = Lane A、B 行 = Lane B）第一个 `[ ]` 未勾项继续执行；完成后立刻把方括号改为 `[x]` 并在备注列写一行。整个文件只允许 Agent 修改自己 lane 下的勾选状态与对应的"备注"列。其余文档按 [explanation.md §2](explanation.md) 的所有权约束。
>
> **Phase A**（需求文档）由 orchestrator 在主对话中完成；下方 A / B 两个 lane 属于 **Phase B**，需用户下达"resume / 恢复执行"指令后由两个 subagent 接管。

---

## Phase A — 需求与规章 (主对话已完成)

- [x] P1. 制定计划文件 `<plan-path>`
- [x] P2. 写 `docs/prd.md`
- [x] P3. 写 `docs/explanation.md`
- [x] P4. 写 `docs/todolist.md`（本文件）
- [ ] P5. 用户下达 "resume / 恢复执行" 指令 → 进入 Phase B

---

## Phase B — <Lane A 名称> Agent
**所有权**：<可写路径列表>。
**绝不动**：<不可写路径列表>。

| 序号 | 任务 | 验收点 | 状态 | 备注 |
|------|------|--------|------|------|
| A1 | <动词开头的小颗粒任务> | <可观察的验收条件> | [ ] |  |
| A2 | … | … | [ ] |  |
| A3 | … | … | [ ] |  |
| … | … | … | [ ] |  |
| A<N-1> | 写 `docs/<contract>.md`（这是 Lane B 的契约） | 文件存在且每端点/字段都有示例 | [ ] |  |
| A<N> | <Lane A 完工签名> | — | [ ] |  |

---

## Phase B — <Lane B 名称> Agent
**所有权**：<可写路径列表>。
**绝不动**：<不可写路径列表>。
**前置**：A<N-1> 完成后 `docs/<contract>.md` 即可读；B 中消费契约的项需等 A<N-1> 通过。其它 B 项可与 Lane A 并行。

| 序号 | 任务 | 验收点 | 状态 | 备注 |
|------|------|--------|------|------|
| B1 | <动词开头的小颗粒任务> | <可观察的验收条件> | [ ] |  |
| B2 | … | … | [ ] |  |
| … | … | … | [ ] |  |
| B<M> | <Lane B 完工签名> | — | [ ] |  |

---

## 完工签名

<!-- Agent 在自己 lane 全部 [x] 后，在此处追加一行：
     `<Lane> lane completed at <YYYY-MM-DDTHH:MM:SS>` -->
